{
  "name": "miaogu-relay",
  "version": "1.0.0",
  "description": "A simple HTTP proxy for Vercel",
  "main": "api/[...slug].js",
  "scripts": {
    "start": "node api/[...slug].js",
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "http-proxy": "^1.18.1"
  },
  "devDependencies": {
    "vercel": "^28.0.0"
  },
  "engines": {
    "node": "22.x"
  }
}
